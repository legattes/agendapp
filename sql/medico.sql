CREATE TABLE Medico(
	medico_id bigint NOT NULL,
    medico_nome varchar(200) NOT NULL,
	medico_cpf varchar(11) NOT NULL,	
	medico_crm varchar(50) NOT NULL,
	medico_telefone varchar(20) NOT NULL,
	medico_email varchar(70)
);